spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/tech_blog # 'tech_blog'? ??? ??? DB ??
    username: root # MariaDB ??? ??
    password: 1234 # MariaDB ?? ? ??? ????

  jpa:
    hibernate:
      ddl-auto: update # ?????? ?? ? DB ???? ???? ????
    properties:
      hibernate:
        show_sql: true # JPA? ???? SQL ??? ??? ???
        format_sql: true # SQL ??? ?? ?? ??